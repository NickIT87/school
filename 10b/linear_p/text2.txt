Для решения этой задачи можно использовать метод линейного программирования. 
Сначала нужно записать целевую функцию и ограничения в стандартной форме:

Целевая функция:

maximize x1 + x2

Ограничения:

4x1 + x2 <= 500
x2 <= 300
x1 + x2 <= 150
4x1 + x2 <= 300

Теперь можно применить симплекс-метод для решения задачи. 
Начнем с записи системы неравенств в виде таблицы:

Базис	x1	x2	s1	s2	s3	s4	b
s1	    4	1	1	0	0	0	500
s2	    0	1	0	1	0	0	300
s3	    1	1	0	0	1	0	150
s4	    4	1	0	0	0	1	300
z	    -1	-1	0	0	0	0	0
Здесь s1, s2, s3 и s4 - это дополнительные переменные, 
которые позволяют привести систему к стандартному виду.

Первоначально, базисными являются переменные s1, s2, s3 и s4, 
а целевая функция имеет значение 0. Начнем с выбора входящей 
переменной (в нашем случае это x1) и исходящей переменной 
(базисной переменной, которая должна покинуть базис).

Сначала вычислим отношение b/s1, b/s2, b/s3 и b/s4 для каждой 
строки таблицы. Выберем строку с наименьшим значением этого 
отношения, в которой входящая переменная (x1) имеет коэффициент 
отличный от нуля. В нашем случае это строка s3, так как b/s3 
имеет наименьшее значение.

Далее нужно преобразовать таблицу так, чтобы переменная x1 
стала базисной переменной в строке s3. Для этого нужно 
разделить всю строку s3 на значение коэффициента при x1 
(то есть 1) и вычесть из нее умноженную на этот коэффициент 
другую строку (например, строку s1). Это д